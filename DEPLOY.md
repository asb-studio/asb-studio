# بردن استودیو روی ورسل

مرحله‌به‌مرحله. کل کار حدود بیست دقیقه است.

---

## قدم ۱ — مخزن گیت‌هاب

استودیو را **در مخزن سایت نگذار.** یک مخزن جدا بساز به اسم `asb-studio`.

چرا: دو چیز مستقل‌اند. اگر بیلد سایت بشکند استودیو نباید بخوابد، و اگر روزی ده بار استودیو را پوش کنی سایت نباید ده بار از نو ساخته شود.

مخزن را **Private** بگذار.

```
asb-studio/
├── index.html
├── vercel.json
├── serve.py
├── README.md  GUIDE.md  DEPLOY.md
├── src/
├── vendor/
├── webfonts/
│   └── Arad-VF.woff2
├── logos/
│   └── asblogo.svg
├── data/
└── supabase/
    └── 01-schema.sql
```

از داخل پوشه:

```
git init
git add .
git commit -m "Asb Studio"
git branch -M main
git remote add origin https://github.com/<username>/asb-studio.git
git push -u origin main
```

> **مهم:** `webfonts/` و `logos/` را حتماً پوش کن. اگر `.gitignore` داری، مطمئن شو `*.woff2` را نادیده نگیرد — وگرنه روی سایت فونت نمی‌آید.

---

## قدم ۲ — ورسل

در داشبورد ورسل: **Add New → Project → Import** و مخزن `asb-studio` را انتخاب کن.

| تنظیم | مقدار |
|---|---|
| Framework Preset | **Other** |
| Build Command | **خالی** بگذار |
| Output Directory | **خالی** بگذار |
| Install Command | **خالی** بگذار |

استودیو فایل ثابت است و هیچ بیلدی لازم ندارد. `vercel.json` هدرهای درست را می‌گذارد.

**Deploy** را بزن. حدود بیست ثانیه.

---

## قدم ۳ — دامنه

در **Settings → Domains** بنویس `studio.asbpub.ir` و اضافه کن.

ورسل یک رکورد CNAME می‌خواهد. چون دامنه‌ات روی کلادفلر است، در بخش DNS کلادفلر:

| نوع | نام | مقدار | پروکسی |
|---|---|---|---|
| CNAME | `studio` | `cname.vercel-dns.com` | **خاموش (DNS only)** |

**آن ابر خاکستری مهم است.** اگر پروکسی روشن باشد، کلادفلر و ورسل هر دو می‌خواهند گواهی SSL بدهند و کار به هم می‌ریزد.

چند دقیقه بعد `https://studio.asbpub.ir` بالا می‌آید.

---

## قدم ۴ — ورود

**بدون این قدم، هرکسی آدرس را بداند وارد می‌شود.** حتماً انجامش بده.

چون DNS روی کلادفلر است ولی پروکسی خاموش است، Cloudflare Access اینجا کار نمی‌کند. راهش **خودِ ورسل** است:

**Settings → Deployment Protection → Password Protection** را روشن کن و یک رمز بگذار.

همان رمز را به دلبر بده. تمام.

> این همان «کلید مخصوص» است که می‌خواستی. یک لایه‌ی ساده جلوی کل سایت.
>
> ورود جداگانه برای هر نفر، **از سوپابیس** می‌آید نه از اینجا — و آن مرحله‌ی بعد است. آن‌وقت می‌فهمی کدام‌یک از شما چه چیزی را ذخیره کرده.

---

## قدم ۵ — بررسی

`https://studio.asbpub.ir` را باز کن و این‌ها را چک کن:

- [ ] رمز می‌پرسد
- [ ] فونت آراد آمده (اگر وزیرمتن دیدی، `webfonts/` پوش نشده)
- [ ] لوگوی اسب وسط صفحه هست
- [ ] `Ctrl+O` فایل باز می‌کند
- [ ] `Ctrl+S` روی همان فایل ذخیره می‌کند

> **یادت باشد:** تنظیماتت منتقل نمی‌شوند. پوسته، چیدمان نوار ابزار و کارنامه در حافظه‌ی مرورگر برای هر آدرس جداست. `localhost:8765` و `studio.asbpub.ir` دو جای متفاوت‌اند.

---

## به‌روزرسانی

هر تغییری که بعداً دادیم:

```
git add .
git commit -m "…"
git push
```

ورسل خودش منتشر می‌کند. دلبر فقط صفحه را رفرش می‌کند.

---

## مرحله‌ی بعد: فایل‌های مشترک

**آنچه تا اینجا داری:** ابزار مشترک. هر دو یک نسخه از استودیو را دارید.

**آنچه هنوز نداری:** فایل‌های مشترک. استودیو همچنان روی دیسک همان کامپیوتر ذخیره می‌کند.

برای آن، `supabase/01-schema.sql` را در SQL Editor سوپابیس اجرا کن و **Authentication → Providers → Email** را روشن کن. بعد Project URL و anon key را بده تا `storage/supabase.js` را بنویسم.

کلید `service_role` **هیچ‌وقت** داخل استودیو نمی‌رود — جایش فقط `.env` روی لپ‌تاپ خودت است.
