# راهنمای استودیوی اسب

این راهنما فرض می‌کند هیچ‌چیز از مارک‌دان بلد نیستی. از صفر شروع می‌کنیم.

---

## ۱. راه انداختن

پوشه‌ی `asb-studio` را باز کن و این را در ترمینال بزن:

```
python serve.py
```

مرورگر خودش باز می‌شود. اگر نشد، برو به `http://localhost:8765`

**چرا نمی‌شود فایل را دابل‌کلیک کرد؟** چون مرورگر برای فایل‌هایی که مستقیم از روی دیسک باز شوند، اجازه‌ی اجرای برنامه نمی‌دهد. `serve.py` یک وب‌سرور کوچک روی همین لپ‌تاپ راه می‌اندازد تا مرورگر خیالش راحت شود. اینترنت لازم ندارد.

برای بستن، در همان ترمینال `Ctrl+C` بزن.

---

## ۲. نگاه اول به صفحه

صفحه پنج بخش دارد. از بالا به پایین:

| بخش | چیست |
|---|---|
| **نوار عنوان** | راست: باز کردن و ذخیره · وسط: نشان نشر |
| **نوار سندها** | هر سند باز یک برگه. با + سند تازه بساز |
| **نوار منو** | پرونده، ویرایش، درج، قالب، بازبینی، ابزار، نما — **همه‌ی فرمان‌ها اینجا هستند** |
| **نوار ابزار** | ابزارهای پرکاربرد به شکل آیکون. هرچه جا نشود می‌رود زیر دکمه‌ی ⋮ |
| **دو ستون وسط** | راست: پیش‌نمایش. چپ: متن خام مارک‌دان |
| **نوار پایین** | تعداد کلمه، شماره‌ی خط، تعداد ایرادها |

نقطه‌ی طلایی روی یک برگه یعنی **آن سند هنوز ذخیره نشده**.

شناسنامه (عنوان، نویسنده، تاریخ و بقیه) دیگر همیشه روی صفحه نیست. در **پنل آماده‌ی انتشار** است که از سمت راست باز می‌شود. سه راه دارد: دکمه‌ی «آماده‌ی انتشار» در گروه بازبینی، `Ctrl+Shift+D`، یا کلیک روی وضعیت پایین صفحه.

منطقش ساده است: متن را ساعت‌ها ویرایش می‌کنی، شناسنامه را دو بار در عمر یک اثر. آنکه همیشه روی صفحه است باید متن باشد.

---

## ۳. باز کردن یک فایل

سه راه:

- دکمه‌ی **باز کردن**
- کلید `Ctrl+O`
- فایل `.md` را با موس بکش و روی صفحه رها کن

بعد از باز شدن، فرم شناسنامه خودش پر می‌شود.

> **مهم:** اگر فایلت فیلدهایی داشته باشد که فرم برایشان جا ندارد (مثل `roles` یا `socials`)، پاک نمی‌شوند. پایین شناسنامه فهرستشان را نشانت می‌دهد و موقع ذخیره سرجایشان برمی‌گردند.

---

## ۴. ذخیره کردن

| کلید | چه می‌کند |
|---|---|
| `Ctrl+S` | ذخیره **روی همان فایلی که باز کردی** |
| `Ctrl+Shift+S` | ذخیره با نام تازه |

روی کروم و اِج، `Ctrl+S` مستقیماً همان فایل را روی دیسک عوض می‌کند. روی فایرفاکس فایل دانلود می‌شود و باید خودت از پوشه‌ی دانلود برداری.

اگر مرورگر ناغافل بسته شد، دفعه‌ی بعد استودیو می‌پرسد که پیش‌نویس را برگرداند یا نه.

---

## ۵. نوشتن متن

در ستون **چپ** می‌نویسی. در ستون **راست** همان لحظه می‌بینی چه شکلی می‌شود.

خط بین دو ستون را می‌شود کشید و عرضشان را عوض کرد.

### پاراگراف
یک خط خالی بین دو پاراگراف بگذار. همین.

### ضخیم و کج
متن را انتخاب کن و `Ctrl+B` یا `Ctrl+I` بزن. یا دکمه‌های گروه **قلم**.

```
**ضخیم**    *کج*    ~~خط‌خورده~~
```

### عنوان
مکان‌نما را روی خط بگذار و دکمه‌ی **ع۱** یا **ع۲** یا **ع۳** را بزن.

### نقل‌قول و فهرست
دکمه‌های گروه **پاراگراف**. دوباره زدن همان دکمه، برش می‌دارد.

---

## ۶. سه چیزی که مخصوص نشر اسب است

اینها را حفظ نکن — دکمه دارند. ولی خوب است بدانی زیرش چه خبر است.

### شعر
مکان‌نما را داخل شعر بگذار و دکمه‌ی **شعر** را بزن. زیر متن این اضافه می‌شود:

```
دانستنِ ما همان ندانستن بود
می‌فهمیدیم اگر نمی‌فهمیدیم
{: .poem }
```

**هیچ‌وقت آخر مصرع بک‌اسلش `\` نگذار.** در سایت ما بک‌اسلش خودش چاپ می‌شود و شکست خط نمی‌سازد. خط‌ها خودبه‌خود حفظ می‌شوند.

### وسط‌چین، بدون تورفتگی، رنگ
همان‌طور. `{: .text-center }` و `{: .no-indent }` و دکمه‌ی رنگ که `{: .color-ochre }` می‌گذارد.

**رنگ روی خودِ پاراگراف می‌نشیند، نه با `<span>`.** استودیوی قبلی برای رنگ کردن یک خط، تگ HTML داخل متن می‌گذاشت. نتیجه‌اش این بود که هیچ ابزاری نمی‌توانست از پشتش متن را بخواند — نه لینتر، نه خروجی ایپاب. حالا فقط یک کلاس در همان فهرست ویژگی است.

### کلاس‌ها و ابزارهای تازه — هماهنگ با راهنمای نوشتن

| دکمه | چه می‌گذارد | برای |
|---|---|---|
| **نقل برجسته** | `{: .pullquote }` | جمله‌ای که باید دیده شود — درشت، وسط، با دو خط اخرایی |
| **ریز** | `{: .small }` | همان‌جا، کمی ریزتر و کم‌رنگ‌تر |
| **امضای ته متن** | `{: .colophon }` | تاریخ و امضا — ریز، کم‌رنگ، سمت چپ |
| **یادداشت ویراستار** | `{: .editor-note }` | حرف درباره‌ی متن — ریز و کج، با خط نازک اخرایی |
| **برجسته** | `<mark>متن</mark>` | هایلایت؛ سایت mark را می‌شناسد |
| **بالانویس / زیرنویس** | `<sup>` / `<sub>` | مارک‌دانِ سایت این دو را ندارد؛ تگ می‌گذارد |
| **فاصله‌ی صحنه** | `<div class="scene-break"></div>` | روی صفحه `* * *` اخرایی؛ در کتاب‌خوان فقط سفیدی. **جای `---` برای فاصله‌ی صحنه** |
| **جعبه‌ی هشدار** | `/// note \| عنوان` | نه رنگ: اخرایی، آبی، قرمز — از روی نوع |
| **بخش تاشو** | `/// details \| عنوان` | افشای پایان و پرسش‌وپاسخ |

پیش‌نمایش همه‌ی این‌ها را همان‌طور که سایت چاپ می‌کند نشان می‌دهد. جزئیات هرکدام در راهنمای نوشتن، بخش‌های شش و نه.

### قانون طلایی ویژگی‌ها
- برای **پاراگراف**: `{: ... }` باید **در خط بعدی و تنها** باشد
- برای **عنوان**: باید **روی همان خط** باشد → `# فصل یک {#chapter-1}`

دقیقاً وارونه‌ی هم‌اند. اگر جابه‌جا بنویسی، سایت متن خام را چاپ می‌کند. ولی نگران نباش: استودیو خودش گیر می‌دهد.

---

## ۷. پانویس

دکمه‌ی **پانویس** در گروه درج شماره را جای مکان‌نما می‌گذارد و **پانل پانویس‌ها** را باز می‌کند تا متنش را همان‌جا بنویسی.

پانل پایین صفحه باز می‌شود، نه در سایدبار — چون پانویس گاهی سه خط است و ستون باریک، آن را به نواری چهارکلمه‌ای تبدیل می‌کند. `Ctrl+Shift+F` یا دکمه‌ی «پانویس‌ها» پایین صفحه.

هر ردیف سه چیز دارد: شماره (کلیک کنی به ارجاعش در متن می‌پری)، متن پانویس که همان‌جا ویرایش می‌شود، و سطل حذف که هم تعریف و هم ارجاع را با هم برمی‌دارد.

```
متن اصلی[^1]

[^1]: احمد اخوت، تا روشنایی بنویس، تهران: جهان کتاب، ۱۴۰۰، ص ۱۶۱.
```

**عمداً شماره‌ها خودبه‌خود مرتب نمی‌شوند.** استودیوی قبلی این کار را می‌کرد و باعث می‌شد با حذف یک پانویس، بقیه یکی جابه‌جا شوند و متن‌ها به شماره‌ی اشتباه بچسبند.

اینجا دکمه‌ی **«مرتب کردن شماره‌ها»** هست که خودت می‌زنی. اول می‌گوید چند تا عوض می‌شود و می‌پرسد. و ارجاع و تعریف را **با هم** عوض می‌کند، پس هیچ‌وقت از هم جدا نمی‌افتند.

پانل خودش هم هشدار می‌دهد: پانویسی که ارجاع ندارد، ارجاعی که تعریف ندارد، تعریف خالی، یا شماره‌ای که با ترتیب متن نمی‌خواند.

**جهت هر پانویس از روی متن خودش تصمیم می‌گیرد.** یک حرف فارسی در متن باشد — هرجا — کادر راست‌چین است؛ پانویس تمام‌لاتین چپ‌چین می‌نشیند و با قلم لاتین، همان‌طور که سایت هم چاپش می‌کند. کادر خالی راست‌چین می‌ماند. انگلیسی که بچسبانی، کادر همان لحظه برمی‌گردد سمت چپ — تنظیمی در کار نیست.

---

## ۸. دو نشانگر ساختاری

اینها معلوم می‌کنند صفحه‌ی سایت از کجا به کجا تقسیم می‌شود. در پیش‌نمایش به شکل خط طلایی دیده می‌شوند.

### شروع متن اثر
```
<!-- EREADER-START -->
```
هرچه **بالایش** باشد، مقدمه‌ی صفحه است. هرچه **پایینش** باشد، متن اثر که در حالت مطالعه خوانده می‌شود.

### دیوار پرداخت
```
<!-- PAYWALL -->
```
فقط برای آثار پولی. هرچه پایینش باشد، فقط برای کسی که خریده.

> **خیلی مهم:** اگر در شناسنامه «اثر پولی» را روشن کنی ولی این نشانگر را نگذاری، `build.py` **کل متن را مسدود می‌کند** و به‌جایش یک هشدار امنیتی چاپ می‌کند. استودیو این را به‌عنوان خطا نشانت می‌دهد.

### بریده‌ی نمایشی
```
<!-- EXCERPT-START -->
دو بند اولِ متن، عیناً.
<!-- EXCERPT-END -->
```
فقط برای آثار کتاب‌خوان‌دار؛ برش، تکرار همان بندهای اول است. **متن بریده را انتخاب کن — یک بند یا ده بند، هرچه خواستی — و دکمه‌ی بریده‌ی نمایشی را بزن:** استارت بالای انتخاب می‌نشیند و اِند پایینش. هیچ متن نمونه‌ای ساخته نمی‌شود.

> **قاعده‌ی طلایی:** بریده همیشه **قبل از** `<!-- PAYWALL -->`. برشی که بعد از دیوار پرداخت باشد، متن پولی را به صفحه‌ی عمومی لو می‌دهد — سیستم جلویش را نمی‌گیرد، ولی استودیو خطای قرمزش می‌کند.

---

## ۹. شناسه‌های دائمی پاراگراف

هر پاراگراف یک شناسه دارد، مثل `#p-2e0b5952`. هایلایت‌ها و جای مطالعه‌ی خواننده‌ها به همین چسبیده‌اند. اگر شناسه عوض شود، هایلایت خواننده گم می‌شود.

دکمه‌ی **شناسه‌ها** در گروه ابزار، به هر پاراگرافی که شناسه ندارد یکی می‌دهد. پاراگراف‌هایی که از قبل شناسه دارند دست نمی‌خورند.

نوار پایین همیشه می‌گوید چند پاراگراف بی‌شناسه مانده.

**دو فرقش با `freeze_ids.py`:**
1. شناسه را همیشه در خط جداگانه می‌گذارد (روی همان خط کار نمی‌کند)
2. به پاراگرافی که با `*` یا `-` شروع شود هم شناسه می‌دهد — پایتون این‌ها را رد می‌کرد

استودیو فقط فایل باز را می‌بیند، پس برای یکتایی در کل مخزن هنوز `freeze_ids.py` مرجع است.

---

## ۱۰. جدول

دکمه‌ی **جدول** در گروه درج. می‌پرسد چند ستون و چند سطر، و اسکلتش را می‌سازد. تایپ کردن `|` در متن راست‌به‌چپ کار عذاب‌آوری است؛ برای همین این دکمه هست.

---

## ۱۱. فهرست مطالب

دکمه‌ی **فهرست** در گروه ساختار:

1. همه‌ی عنوان‌ها را پیدا می‌کند
2. به هرکدام که لنگر ندارد، یکی می‌دهد
3. یک فهرست لینک‌دار بالای فایل می‌سازد

اول عنوان‌هایت را بساز، بعد این دکمه را بزن.

---

## ۱۲. جست‌وجو و جایگزینی

| کلید | کار |
|---|---|
| `Ctrl+F` | جست‌وجو |
| `Ctrl+H` | جست‌وجو و جایگزینی |
| `Enter` | مورد بعدی |
| `Esc` | بستن |

تطابق حروف بزرگ و کوچک، کلمه‌ی کامل و عبارت باقاعده (regex) هم دارد.

---

## ۱۳. فهرست ایرادها

پایین صفحه، سمت چپ. رویش کلیک کنی باز می‌شود.

دو نوع دارد:

- **خطا** (قرمز) — این در سایت خراب درمی‌آید. حتماً درستش کن.
- **هشدار** (زرد) — احتمالاً اشتباه است، ولی سایت را نمی‌شکند.

هر ایراد سه چیز نشان می‌دهد: کجاست، الان چه شکلی است، و باید چه شکلی باشد. روی هر ایراد کلیک کنی، مکان‌نما به همان خط می‌پرد.

---

## ۱۴. ظاهر

- منوی **پوسته**: چهار انتخاب — کاغذ، سفید، خاکستری، شب. کنار پوسته‌ی فعلی تیک می‌بینی و یادش می‌ماند.
- منوی **نما**: دو ستونی، فقط متن، فقط پیش‌نمایش — با تیک روی حالت فعلی
- خط بین دو ستون را می‌شود کشید و عرضشان را عوض کرد
- دکمه‌ی اول نوار عنوان، شناسنامه را باز و بسته می‌کند
- کارت‌های شناسنامه جمع می‌شوند و یادشان می‌ماند

**فونت:** استودیو دنبال `webfonts/Arad-VF.woff2` می‌گردد. اگر نبود، وزیرمتن را می‌گذارد.

---

## ۱۵. همه‌ی کلیدها یک‌جا

داخل خود استودیو هم هست: کلید `F1` یا دکمه‌ی کیبورد بالا سمت چپ.


| کلید | کار |
|---|---|
| `Ctrl+O` | باز کردن فایل |
| `Ctrl+S` | ذخیره |
| `Ctrl+Shift+S` | ذخیره با نام تازه |
| `Ctrl+B` | ضخیم |
| `Ctrl+I` | کج |
| `Ctrl+K` | پیوند |
| `Ctrl+F` | جست‌وجو |
| `Ctrl+H` | جایگزینی |
| `Ctrl+Z` | واگرد |
| `Ctrl+Y` | ازنو |
| `Alt+↑` و `Alt+↓` | جابه‌جا کردن خط |
| `Ctrl+Alt+1` تا `4` | نقل‌قول‌های خمیده: ‘ ’ “ ” — ردیف بالای صفحه یا ناپد، هر دو |
| `F1` | کارت کلیدهای میان‌بر |
| `Esc` | بستن پنجره‌ها |

---

## ۱۶. بررسی سلامت فایل‌ها

در «مرور آرشیو» (منوی پرونده)، استودیو هر فایل را باز و بسته می‌کند و مطمئن می‌شود **بایت‌به‌بایت** همان می‌ماند.

اگر فایلی از این آزمون رد نشود، با نشان قرمز **ناسالم** علامت می‌خورد. یعنی ویرایش کردنش با استودیو خطرناک است و باید اول بگویی تا ببینم چه چیزی در آن فایل غیرعادی است.

فایل ناسالم در کارهای گروهی هم اصلاً شرکت داده نمی‌شود.

## ۱۷. اگر چیزی خراب شد

**صفحه سفید و خام است**
یعنی `index.html` سرجایش نیست. باید بیرونِ همه‌ی پوشه‌ها باشد، کنار `serve.py`.

**فونت عوض نشده**
`webfonts/Arad-VF.woff2` را چک کن. اسم باید دقیقاً همین باشد.

**`Ctrl+S` دانلود می‌کند به‌جای ذخیره**
فایرفاکسی. کروم یا اِج باز کن.

**پیش‌نمایش با سایت فرق دارد**
این یعنی ما جایی رفتار `build.py` را اشتباه تقلید کرده‌ایم. بگو تا درستش کنیم — پیش‌نمایشی که دروغ بگوید، از نداشتن پیش‌نمایش بدتر است.


---

## ۱۸. پنل آماده‌ی انتشار

`Ctrl+Shift+D` یا دکمه‌ی «آماده‌ی انتشار».

از سمت راست باز می‌شود و سه چیز دارد:

۱. **گزارش آمادگی** — بالای پنل. می‌گوید چه چیزی مانع انتشار است و چه چیزی فقط هشدار. مثلاً اسلاگ با فاصله، اثر پولی بدون قیمت، تاریخ با قالب اشتباه، خانه‌ی خالی در شناسنامه، یا بریده‌ی نمایشی که بعد از دیوار پرداخت افتاده باشد.
۲. **شناسنامه** — همان فرم قبلی، با همه‌ی کارت‌هایش.
۳. **ذخیره** — پایین پنل.

نوار پایین صفحه هم همیشه می‌گوید آماده‌ی انتشار هستی یا نه. رویش کلیک کنی، پنل باز می‌شود.

---

## ۱۹. تعمیر خودکار

دکمه‌ی **تعمیر** در گروه بازبینی. چهار چیز را یک‌جا درست می‌کند:

- بک‌اسلش انتهای خط
- ویژگی پاراگراف که روی همان خط نوشته شده
- ویژگی عنوان که در خط جدا نوشته شده
- نحو پندوکی `[متن]{.کلاس}`

اول نشانت می‌دهد چند تا از هرکدام پیدا کرده و می‌پرسد. با `Ctrl+Z` هم برمی‌گردد.

(قاعده‌ی قدیمی «منابع ← سرچشمه‌ها» برداشته شده: طبق راهنمای نوشتن، سایت هر پنج عنوان — سرچشمه، سرچشمه‌ها، کتابنامه، کتاب‌نامه، منابع — را قبول می‌کند، پس «منابع» غلط نبود که تعمیرش کنیم.)

فقط چیزهایی اینجا هستند که **یک جواب درست قطعی** دارند. هر چیزی که سلیقه بخواهد، عمداً بیرون گذاشته شده — ابزاری که حدس بزند نویسنده چه می‌خواسته، بالاخره یک روز اشتباه حدس می‌زند و بی‌صدا.

---

## ۲۰. آمار ویراستاری

دکمه‌ی **آمار** پایین صفحه، یا در گروه بازبینی.

- کلمه، پاراگراف، جمله، دقیقه‌ی مطالعه
- **میانگین طول جمله** و بلندترین جمله
- **پاراگراف‌های بلندتر از ۱۲۰ کلمه** — رویشان کلیک کنی، به همان خط می‌پری
- **کلمه‌هایی که در فاصله‌ی نزدیک تکرار شده‌اند** — کلمه‌های خیلی رایج حساب نمی‌شوند

نشانه‌گذاری مارک‌دان از شمارش کنار گذاشته می‌شود، پس عددها مال نثر است نه علامت‌ها.

---

## ۲۱. نمای کاغذ

منوی **نما → نمای کاغذ**.

پیش‌نمایش معمولاً تمام پنجره را پر می‌کند — که هیچ ربطی به عرض واقعی صفحه‌ی سایت ندارد. در نمای کاغذ، متن به **همان عرضی** درمی‌آید که خواننده می‌بیند، روی یک برگ با حاشیه‌ی واقعی و زمینه‌ی تیره‌تر پشتش.

به چه درد می‌خورد: طول سطرها، جای شکستن پاراگراف‌ها، و اینکه یک نقل‌قول یا شعر در صفحه چطور می‌نشیند. چیزهایی که در پنجره‌ی عریض هیچ‌وقت معلوم نمی‌شوند.


---

## ۲۲. چند سند باز

نوار زیر عنوان، برگه‌های سندهای باز است. دکمه‌ی **+** سند تازه می‌سازد.

| کلید | کار |
|---|---|
| `Ctrl + Alt + ←` و `→` | سند بعدی و قبلی |
| `Ctrl + Alt + W` | بستن سند |

(پرش شماره‌ای به سند حذف شد — چهار کلیدش را نقل‌قول‌های خمیده گرفتند. فلش‌ها می‌چرخانند.)

از `Ctrl+W` و `Ctrl+Tab` استفاده نکردیم چون مال خود مرورگرند. از `Alt+←/→` هم نه، چون در کروم یعنی «برگرد» و «برو جلو». `Ctrl+Alt` آزاد است.

هر سند جای مکان‌نمای خودش را یادش می‌ماند. برگردی، دقیقاً همان‌جایی هستی که رهایش کردی.

اگر فایلی از قبل باز باشد، دوباره بازش نمی‌کند — می‌رود سراغ همان برگه. دو برگه روی یک فایل، راهِ گم شدن ویرایش است.

**اگر مرورگر ناغافل بسته شد**، دفعه‌ی بعد کارتی می‌آید و می‌گوید کدام سندها ذخیره‌نشده بودند، هرکدام چند کلمه دارد و چند ایراد. یکی‌یکی نه — همه با هم برمی‌گردند.

---

## ۲۳. کارنامه‌ی کار

دکمه‌ی **کارنامه** پایین صفحه. می‌گوید روی چند سند کار کرده‌ای، چند ساعت، چند بار ذخیره، و چهارده روز اخیر چطور بوده.

**صادقانه بدان چه چیزی هست و چه چیزی نیست:** این فقط در همین مرورگر و روی همین لپ‌تاپ نگه داشته می‌شود. داده‌های مرورگر را پاک کنی، از بین می‌رود. مرورگر دیگر یا لپ‌تاپ دیگر، کارنامه‌ی جدای خودش را دارد. **آمار مشترک نشر نیست، کارنامه‌ی کار توست.**

زمان هم فقط وقتی شمرده می‌شود که واقعاً تایپ می‌کنی. تبِ بازِ رهاشده تا صبح، یک دقیقه هم اضافه نمی‌کند.


---

## ۲۴. خروجی EPUB

دکمه‌ی **ایپاب** در گروه خروجی.

دو سؤال می‌پرسد:

**۱. هر فصل از کجا شروع شود؟** از عنوان‌های سطح یک (`#`)، سطح دو (`##`)، یا کل متن در یک فصل. فهرست کتاب از روی همین ساخته می‌شود.

**۲. چه مقدار از متن؟** اگر دیوار پرداخت داشته باشی، می‌پرسد کل کتاب را می‌خواهی یا فقط تا دیوار پرداخت — یعنی همان نمونه‌ی رایگانی که می‌شود جای دیگر پخش کرد. فایل نمونه با پسوند `-sample.epub` ساخته می‌شود.

کتاب راست‌به‌چپ ساخته می‌شود، با صفحه‌ی عنوان، فهرست مطالب، و پانویس‌هایی که در اپل بوکس و توریوم به شکل حباب باز می‌شوند نه اینکه خواننده را ته کتاب پرت کنند.

**نکته‌ی مهم:** کتاب از **مارک‌دان** ساخته می‌شود، نه از HTML سایت و نه از JSON پولی. آن دو تا هم مقصدند؛ ساختن کتاب از یکی‌شان یعنی ساختن کپیِ کپی، و هر عیبی که در آن مرحله باشد به کتاب هم می‌رسد.

پانویس‌ها هم موقع تقسیم به فصل، **با فصل خودشان می‌روند**. تعریف‌ها ته فایل‌اند ولی ارجاع‌ها در سراسر متن؛ اگر ساده تقسیم می‌کردیم، هر ارجاع تعریفش را گم می‌کرد.


---

## ۲۵. تبدیل فایل‌های قدیمی

دکمه‌ی **تبدیل فایل قدیمی** در گروه بازبینی.

فایل‌هایی که با استودیوی قبلی ساخته شده‌اند پر از HTML دست‌ساز‌اند. این دکمه آن‌ها را به مارک‌دان خالص برمی‌گرداند:

| قدیم | جدید |
|---|---|
| `<sup class="fn-ref"><a href="#fn1" id="ref1">۱</a></sup>` | `[^1]` |
| کل بلوک `<div class="footnotes-container">` | `[^1]: متن پانویس` |
| `***` و `### پانویس‌ها` بالای آن بلوک | حذف — پایتون خودش می‌سازدشان |
| `<span class="color-ochre">متن</span>` | کلاس در فهرست ویژگی همان پاراگراف |
| `<div class="page-break"></div>` | `***` |

اول نشانت می‌دهد چند تا از هرکدام پیدا کرده، **و مهم‌تر: اگر HTMLی مانده باشد که نمی‌شناسد، فهرستش می‌کند** تا خودت نگاه کنی. چیزی بی‌صدا تبدیل نمی‌شود و چیزی هم بی‌صدا جا نمی‌ماند.

با `Ctrl+Z` برمی‌گردد. شناسه‌های `#p-` دست‌نخورده می‌مانند، پس هایلایت خواننده‌ها سرجایشان است.

**چرا این‌قدر مهم است:** تا وقتی پانویس‌ها HTML‌اند، هیچ ابزاری نمی‌بیندشان — نه پانل پانویس، نه لینتر، نه خروجی ایپاب. و برای اضافه کردن یک پانویس وسط متن، باید یازده تا شماره را دستی عوض کنی.


---

## ۲۶. نمایشگر کوچک

نوار ابزار خودش را با عرض پنجره اندازه می‌گیرد. اگر جا کم بیاید، ابزارها **ناپدید نمی‌شوند** — می‌روند زیر دکمه‌ی **⋮** سمت چپ نوار.

و هر فرمانی که در نوار ابزار هست، در **نوار منو** هم هست. منو هیچ‌وقت جا کم نمی‌آورد. پس روی هر نمایشگری، به همه‌چیز دسترسی داری.

---

## ۲۷. ردیاب تغییرات

علامت‌گذاری دستی برداشته شد. ردیاب مثل ورد یک کلید است: روشنش می‌کنی و عادی ویرایش می‌کنی — خودش پیدا می‌کند چه عوض شده. همه‌ی جزئیاتش در بخش ۳۸.

تنها چیزی که دستی مانده **یادداشت برای نویسنده** است: متن را انتخاب کن و از منوی بازبینی بزن. داخل خود فایل `{>>حرف من<<}` می‌نویسد.

**چرا داخل خود فایل؟** چون آن‌وقت گیت می‌بیندش، فایل را می‌شود به نویسنده داد تا در همین استودیو بازش کند، و اگر روزی سروری از دست برود چیزی گم نمی‌شود. علامتی که فقط در حافظه‌ی یک برنامه باشد، بالاخره یک روز گم می‌شود.

### پانل تغییرات

`Ctrl+Shift+R` یا دکمه‌ی «تغییرها» پایین صفحه.

هر تغییر یک ردیف: چه نوعی است، چه بود و چه می‌شود، در کدام خط. و دو دکمه: **بپذیر** و **رد کن**. بالای پانل هم «پذیرفتن همه» و «رد کردن همه» هست.

هیچ‌چیز خودکار رسیدگی نمی‌شود. علامت ویراستاری یک **سؤال** است که از آدمی پرسیده شده؛ کار استودیو آسان کردن جواب دادن است، نه جواب دادن.

### مهم

تا وقتی علامتی مانده باشد، **استودیو نمی‌گذارد منتشرش کنی**. در گزارش آمادگی خطای قرمز می‌بینی. علتش این است که سایت این علامت‌ها را نمی‌شناسد و خام چاپشان می‌کند. اول رسیدگی کن، بعد منتشر.


---

## ۲۸. جدول‌ساز

منوی **درج → جدول**.

جدول را روی صفحه می‌بینی و همان‌جا پرش می‌کنی. تعداد ستون و سطر با دکمه‌های + و −، ترازِ هر ستون جدا (راست، وسط، چپ، پیش‌فرض)، و پایین صفحه مارک‌دانی که ساخته می‌شود زنده به‌روز می‌شود.

تراز «چپ» برای ستون‌هایی به کار می‌آید که محتوایشان لاتین است — مثل عنوان اصلی کتاب یا نام نویسنده به انگلیسی.

---

## ۲۹. مرور آرشیو

منوی **پرونده → مرور آرشیو**. فقط روی کروم و اِج.

یک بار پوشه‌ی `main/` را انتخاب می‌کنی و استودیو **همه‌ی فایل‌های `.md` زیرش** را می‌خواند و می‌گوید هرکدام چه وضعی دارد:

| نشان | یعنی |
|---|---|
| **ناسالم** | باز و بسته کردنش فایل را عوض می‌کند — با استودیو ویرایشش نکن |
| **قدیمی** | HTML دست‌ساز استودیوی قبلی دارد و باید تبدیل شود |
| **نام جدا** | نویسنده یا مترجمش به هیچ صفحه‌ی پدیدآورنده‌ای وصل نمی‌شود |
| **ایراد** | چیزی در سایت خراب درمی‌آید یا مانع انتشار است |
| **تغییر باز** | علامت ردیاب تغییرات دارد که رسیدگی نشده |
| **هشدار** | پاراگراف بی‌شناسه یا هشدار دیگر |
| **سالم** | چیزی لازم ندارد |

روی هر فایل کلیک کنی، در یک برگه‌ی تازه باز می‌شود.

### کارهای گروهی

بالای فهرست سه دکمه هست: **تبدیل فایل‌های قدیمی**، **تعمیر خودکار**، **دادن شناسه**. هرکدام روی همه‌ی فایل‌هایی که لازمش دارند اجرا می‌شود.

**قبلش حتماً در گیت کامیت کن.** این کار روی دیسک می‌نویسد و `Ctrl+Z` ندارد. استودیو هم اول فهرست فایل‌هایی را که دست می‌زند نشانت می‌دهد و می‌پرسد، و آخرش دقیقاً می‌گوید چه کرد.

---

## ۳۰. دسته‌بندی‌ها از کجا می‌آیند

استودیو فهرست دسته‌بندی‌ها را از `data/categories.json` می‌خواند — فایلی که خودِ `build.py` از روی `CATEGORY_CONFIG` می‌سازد.

چرا: مرورگر پایتون نمی‌خواند، پس تا دیروز این فهرست یک رونوشت دستی بود. دو فهرستی که باید مثل هم بمانند و در دو جا ویرایش می‌شوند، بالاخره از هم جدا می‌افتند.

اگر آن فایل نباشد، فهرست داخلی استودیو کار می‌کند و چیزی خراب نمی‌شود.


---

## ۳۱. صفحه‌ی پدیدآورنده

نشر دو جور سند دارد و `build.py` با آن‌ها فرق می‌گذارد:

- **اثر** — داستان، جستار، مطلب مجله. عنوان و نویسنده و دسته و تاریخ دارد.
- **پدیدآورنده** — صفحه‌ی یک آدم. نام و نقش و شبکه‌ها دارد، و متنی که می‌نویسی **زندگی‌نامه‌ی اوست**.

استودیو خودش تشخیص می‌دهد کدام است، و بالای پنل «آماده‌ی انتشار» هم می‌بینی و می‌توانی دستی عوضش کنی.

### وقتی «پدیدآورنده» باشد

فرم عوض می‌شود:

| بخش | چه چیزی |
|---|---|
| **پدیدآورنده** | نام و اسلاگ |
| **نقش‌ها** | نویسنده، مترجم، ویراستار، شاعر، تصویرگر… روی هرکدام کلیک کنی انتخاب می‌شود. نقش تازه هم می‌شود اضافه کرد |
| **شبکه‌ها** | وب‌سایت، ایکس، تلگرام، اینستاگرام، لینکدین، گودریدز، ایمیل — خالی‌ها اصلاً در فایل نوشته نمی‌شوند |
| **عکس** | نام فایل عکس |

و دیگر سراغ عنوان و تاریخ و دسته و قیمت را نمی‌گیرد، چون صفحه‌ی پدیدآورنده این‌ها را ندارد.

### دو نکته

**نام، کلید وصل شدن آثار است.** `build.py` آثار را با تطبیق **دقیق نام** به این صفحه وصل می‌کند. اگر در فایل داستان نوشته‌ای «امیرمحمد شیرازیان» و اینجا «امیرمحمد شیرازیان » با یک فاصله‌ی اضافه، وصل نمی‌شود.

**مرور آرشیو همین را می‌گیرد.** چون همه‌ی فایل‌ها را با هم می‌بیند، نام نویسنده و مترجم و ویراستار هر اثر را با نام صفحه‌های پدیدآورنده می‌سنجد. اگر جایی نخواند، نشان **«نام جدا»** می‌گیرد — و اگر نامِ نزدیکی پیدا کند، می‌گوید احتمالاً منظورت کدام بوده.

**مسیر عکس.** `build.py` عکس را کنار خود فایل `.md` یا یک پوشه بالاتر می‌گردد. اگر مسیر با `/` شروع شود، هیچ‌کدام را پیدا نمی‌کند. استودیو در این حالت هشدار می‌دهد — یک بار صفحه‌ی آن پدیدآورنده را روی سایت ببین و مطمئن شو عکس می‌آید.


---

## ۳۲. شناسنامه‌ی استاندارد

هر بار که ذخیره می‌کنی، شناسنامه به ترتیب **راهنمای نوشتن** درمی‌آید:

```yaml
---
book_id: a04fb6c7-0d3d-4b52-abb3-6e52c220e7c6
title: ویکی‌تاریخ
slug: wikihistory
author: دزموند وُرزل
translator: سیدمحمدرضا ایزدپناه
editor: امیرمحمد شیرازیان
category: short-story/single
series: نام مجموعه
language: fa
reader: true
premium: false
date: "1405-04-12"
cover: wikihistory-cover.jpg
tags: [علمی‌تخیلی, داستان کوتاه]
summary: "یکی دو بند درباره‌ی اثر."
---
```

قواعدش — همه از راهنمای نوشتن:

- **خانه‌ی خالی هرگز نوشته نمی‌شود.** `build.py` فایلِ کلید خالی را رد می‌کند؛ پس فقط فیلدهای ساختاری (book_id، category، language، reader، premium، date) همیشه هستند، و بقیه فقط وقتی مقدار دارند. خالی‌های قدیمی هم موقع مرتب کردن پاک می‌شوند.
- **تاریخ و ساعت همیشه گیومه‌دار** — `"1405-04-12"` و `"20:00"`. بدون گیومه، YAML آن‌ها را عدد و تاریخ می‌بیند نه رشته.
- **فیلدهایی که استودیو نمی‌شناسد دست نمی‌خورند** و بعد از بلوک استاندارد سرجایشان برمی‌گردند.
- کارت‌های پنل هم هوشمند شده‌اند: فیلدهای پیش‌خرید فقط وقتی release_date گذاشته باشی می‌آیند، و قیمت‌ها فقط با «اثر پولی».

اگر خواستی بدون ذخیره کردن مرتبش کنی: **ابزار → مرتب کردن شناسنامه**. اول نشان می‌دهد چه اضافه می‌شود، چه خانه‌های خالی‌ای پاک می‌شود، و کدام تاریخ‌ها گیومه می‌گیرند — و می‌پرسد.

> **نکته:** اولین بار که یک فایل قدیمی را ذخیره کنی، ترتیب شناسنامه‌اش عوض می‌شود و خالی‌هایش می‌روند. همین چیزی است که می‌خواستی.

---

## ۳۳. چیدمان نوار ابزار

**نما → چیدمان نوار ابزار**.

هر ابزاری که تیک بخورد روی نوار می‌آید. هرچه جا نشود می‌رود زیر ⋮. یادش می‌ماند.

پس اگر مثل من هر روز سراغ `<!-- EREADER-START -->` می‌روی، تیکش را بزن تا همیشه جلوی چشمت باشد. و اگر ابزاری را هیچ‌وقت استفاده نمی‌کنی، تیکش را بردار — از منو که همیشه در دسترس است.

---

## ۳۴. نام فایل

نام فایل روی دیسک، **همان چیزی است که در آدرس صفحه می‌آید**.

پس اگر فایلی به اسم `geever's-flight.md` بسازی، آپاستروف در آدرس به `%27` تبدیل می‌شود و لینک به چیزی درمی‌آید که نه می‌شود خواندش نه در پیام فرستادش.

استودیو حالا هشدار می‌دهد و **اسم درست را هم پیشنهاد می‌دهد**. فقط حروف کوچک انگلیسی، رقم، و خط تیره.

«مرور آرشیو» هم همین را روی کل پوشه چک می‌کند.


---

## ۳۵. سرچشمه‌ها

دکمه‌ی **سرچشمه‌ها** پایین صفحه، یا منوی **درج → سرچشمه‌ها**.

هر سرچشمه یک ردیف. «افزودن سرچشمه» ردیف تازه می‌سازد، سطل حذفش می‌کند، و شماره‌گذاری خودکار است.

عنوان بخش (`### سرچشمه`) را هم خودش می‌سازد و جایش را درست می‌گذارد — **بالای تعریف پانویس‌ها**، چون `build.py` بلوک پانویس را می‌برد بالای همین عنوان. اگر جای این دو عوض شود، در صفحه‌ی نهایی هم عوض می‌شوند.

عنوان‌های معتبر پنج‌تاست — **سرچشمه، سرچشمه‌ها، کتابنامه، کتاب‌نامه، منابع** — و پانل هر پنج را می‌شناسد. هشدار قدیمی که «منابع» را غلط می‌گرفت برداشته شده؛ `build.py` خودش هر پنج عنوان را قبول می‌کند.

### قالب‌بندی داخل ردیف‌ها

زیر هر کادر — چه در پانویس‌ها چه در سرچشمه‌ها — چهار دکمه‌ی کوچک هست: **ض** ضخیم، **ک** کج، **⌗** کد، **↗** پیوند.

کلیدهای `Ctrl+B` و `Ctrl+I` و `Ctrl+K` هم داخل همان کادرها کار می‌کنند.

پس برای نوشتن `*Ernest Hemingway on Writing*` دیگر لازم نیست ستاره‌ها را دستی بشماری.

---

## ۳۶. اصلاح با یک کلیک

در پنل ایرادها، هر موردی که استودیو بلد باشد خودش درستش کند، یک دکمه‌ی **«اصلاح کن»** دارد.

بک‌اسلش انتهای خط، ویژگی روی خط اشتباه، نحو پندوکی، و فرانت‌متری که داخل متن افتاده — همه یک‌کلیکی‌اند. با `Ctrl+Z` هم برمی‌گردند.

مواردی که دکمه ندارند، آن‌هایی‌اند که یک جواب درستِ قطعی ندارند و باید خودت تصمیم بگیری.


---

## ۳۷. حساب کاربری و فضای مشترک

**ورود اختیاری است.** استودیو بدون حساب هم کامل کار می‌کند — می‌نویسی، ذخیره می‌کنی، همه‌چیز روی دیسک خودت. حساب فقط **فضای مشترک** را باز می‌کند.

دکمه‌ی **ورود | ثبت‌نام** پایین صفحه، یا منوی **پرونده → حساب کاربری**.

**رمزی در کار نیست.** نه می‌سازی، نه ذخیره می‌شود، نه فراموش می‌کنی. هر بار یک کد یک‌بارمصرف می‌آید.

### ثبت‌نام (یک بار)

نام و ایمیل. کد می‌آید، می‌نویسی، تمام.

**نام مهم است** — همان چیزی است که دلبر کنار سندهای باز می‌بیند. ایمیل به کسی نمی‌گوید تو کیستی.

### ورود (هر بار بعدش)

فقط ایمیل. کد می‌آید، می‌نویسی. نامت خودش برمی‌گردد.

### کی آنلاین است

وقتی هر دو وارد باشید، پایین صفحه اسم آن یکی را با یک نقطه‌ی سبز می‌بینی.

این وضعیت در هیچ جدولی ذخیره نمی‌شود — به خودِ اتصال بسته است. یعنی وقتی لپ‌تاپ بسته می‌شود، بلافاصله درست است. ردیفی در پایگاه‌داده که بگوید «فلانی آنلاین است»، همان لحظه‌ای که برق برود دروغ می‌شود.

### فرستادن یک سند

**پرونده → فرستادن به فضای مشترک.** مسیرش را می‌پرسد — همان مسیری که فایل در پوشه‌ی سایت دارد، مثل `main/short-story/single/…/wikihistory.md`. آن مسیر، **هویت سند** است.

### باز کردن

**پرونده → فضای مشترک.** فهرست همه‌ی سندها را می‌بینی، با اینکه هرکدام را چه کسی آخرین بار ذخیره کرده و کِی.

روی هرکدام کلیک کنی باز می‌شود و **قفلش را می‌گیری**.

### قفل

وقتی سندی را باز می‌کنی، تا سی دقیقه دست توست. دلبر همان لحظه می‌بیند «دست امیرمحمد» و می‌تواند بازش کند ولی **فقط برای خواندن** — ذخیره‌اش نمی‌شود.

تا وقتی سند باز است، قفل خودش تمدید می‌شود. وقتی برگه را ببندی، قفل آزاد می‌شود.

**قفل، نه ویرایش هم‌زمان.** شما یک پاراگراف را در یک ثانیه تایپ نمی‌کنید؛ نوبتی کار می‌کنید. قفل همان نوبت را از یک توافق تبدیل می‌کند به چیزی که ابزار رعایتش می‌کند.

### و ردیاب تغییرات، اینجا سودش را نشان می‌دهد

دلبر سندی را ویرایش می‌کند و علامت می‌گذارد. تو بازش می‌کنی و **همه‌ی علامت‌هایش را می‌بینی** — چون علامت‌ها داخل خود متن‌اند. هیچ سازوکار همگام‌سازی جداگانه‌ای لازم نیست.

### انتشار

فضای مشترک جای **کار کردن** است، نه انتشار. وقتی متن آماده شد، بازش کن، `Ctrl+Shift+S` بزن و در پوشه‌ی سایت ذخیره‌اش کن. بعد `freeze_ids.py` و `build.py` و پوش.


---

## ۳۸. ردیاب تغییرات

مثل ورد کار می‌کند: **روشنش می‌کنی و عادی ویرایش می‌کنی.** لازم نیست چیزی را دستی علامت بزنی.

### روشن کردن

منوی **بازبینی → ردیابی تغییرات**، یا `Ctrl+Shift+E`.

لحظه‌ای که روشن می‌کنی، استودیو **یک نسخه از متن را نگه می‌دارد**. از آن به بعد هر چیزی که عوض کنی — یک کلمه، یک جمله، یک پاراگراف کامل — با مقایسه‌ی همان نسخه با متن فعلی پیدا می‌شود.

پایین صفحه می‌بینی: **«ردیاب روشن · ۱۲ تغییر»**. عدد زنده به‌روز می‌شود.

### دیدن تغییرها روی متن

تا وقتی ردیاب روشن است، **پیش‌نمایش خودش تغییرها را رنگی نشان می‌دهد** — افزوده سبز، حذف قرمز و خط‌خورده. همان «All Markup» ورد.

از منوی **بازبینی → نمایش تغییرات در پیش‌نمایش** می‌شود خاموشش کرد تا متن نهایی را ببینی. همان «No Markup».

### پانل تغییرات

`Ctrl+Shift+R`. فهرست هر چیزی که عوض شده، و **هرکدام دکمه‌ی پذیرش و رد خودش را دارد** — مثل ورد.

- **بپذیر** — این تغییر درست است، دیگر تغییر شمرده نمی‌شود. متن دست نمی‌خورد.
- **رد کن** — این تغییر اشتباه بود، متن همان‌جا به شکل قبلی‌اش برمی‌گردد.
- **نشانم بده** — مکان‌نما را می‌برد سر همان جا.

بالای پانل هم چهار دکمه‌ی گروهی هست، دقیقاً مثل منوی Accept و Reject ورد:

| دکمه | کارش |
|---|---|
| پذیرش همه | همه‌ی تغییرها پذیرفته می‌شوند. متن دست نمی‌خورد |
| رد همه | متن به همان شکلی برمی‌گردد که ردیاب روشن شد |
| پذیرش همه و توقف | همان بالا، و ردیاب خاموش می‌شود |
| رد همه و توقف | همان بالا، و ردیاب خاموش می‌شود |

### یادداشت برای نویسنده

تنها چیزی که هنوز دستی است — چون یادداشت **تغییر متن نیست**، حرفی است درباره‌ی متن، و هیچ مقایسه‌ای نمی‌تواند حدسش بزند.

متن را انتخاب کن و از منوی بازبینی «یادداشت برای نویسنده» را بزن.

### فرق پذیرش و رد

این را یک بار بفهمی، بقیه‌اش روشن است:

- **پذیرش** یعنی «متن جدید درست است» → نسخه‌ی مبنا جلو می‌آید تا با متن بخواند. **متن تغییر نمی‌کند.**
- **رد** یعنی «متن قدیمی درست بود» → متن به شکل قبلی‌اش برمی‌گردد. **مبنا تغییر نمی‌کند.**

هر دو با `Ctrl+Z` هم برمی‌گردند.

**مبنا کجا زندگی می‌کند:** سندِ فضای مشترک، مبنایش با مسیرش ذخیره می‌شود و بعد از بستن مرورگر هم می‌ماند — و خاموش کردن ردیاب، آن نسخه را از فضای مشترک هم پاک می‌کند تا فردا سند با ردیابِ روشن باز نشود. فایل محلی برعکس: مبنایش فقط تا بستن مرورگر است، تا مبنی از پرونده‌ای هرگز روی پرونده‌ی هم‌نام روشن نشود.

---

## ۳۸.۱ گزارش برای پدیدآورنده

منوی **بازبینی → گزارش برای پدیدآورنده**، یا دکمه‌اش در پانل تغییرات.

یک فایل HTML **کاملاً مستقل** می‌سازد — نه سرور می‌خواهد، نه نصب چیزی. روی گوشی باز می‌شود و می‌شود ضمیمه‌ی ایمیل کرد.

**دو حالت دارد، مثل ورد:**

- **نمایش تغییرات** — همه‌چیز رنگی: افزوده‌ها سبز و زیرخط‌دار، حذف‌ها قرمز و خط‌خورده
- **متن نهایی** — متن همان‌طور که منتشر می‌شود، بدون هیچ علامتی

نویسنده با یک دکمه بینشان جابه‌جا می‌شود.

**یادداشت‌ها در هر دو حالت هستند** — به شکل یک دایره‌ی کوچک اخرایی کنار همان کلمه. رویش بزند، حرفت را می‌بیند.

بالای صفحه: لوگوی نشر، عنوان اثر، نام نویسنده و مترجم، شمارش تغییرها، و راهنمایی که توضیح می‌دهد رنگ‌ها یعنی چه. پایینش: **تحریریه‌ی نشر اسب**.

وزیرمتن، زمینه‌ی کاغذی، واکنش‌گرا، و با `Ctrl+P` تمیز چاپ می‌شود.

---

## ۳۹. روی موبایل

استودیو حالا روی صفحه‌های کوچک هم کار می‌کند:

- **یک ستون در هر لحظه.** با منوی نما بین متن و پیش‌نمایش جابه‌جا شو.
- نوار منو افقی اسکرول می‌شود؛ همه‌ی فرمان‌ها هست.
- برچسب دکمه‌های بالا می‌رود، آیکون‌ها می‌مانند — و هرکدام در منو با اسم کامل هست.
- کشوها تا نصف صفحه باز می‌شوند و **متن را نمی‌پوشانند** — پنجره واقعاً کوچک می‌شود.

برای نوشتن جدی هنوز لپ‌تاپ بهتر است، ولی برای خواندن و تأیید تغییرات روی موبایل کاملاً کار می‌کند.
